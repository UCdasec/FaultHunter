package com.afivd.afivd;

public class DefaultFail extends CBaseListener{
    private ParsedResults output;
    private CParser parser;

    public DefaultFail(CParser parser, ParsedResults output){
        this.parser = parser;
        this.output = output;
    }
    // A tree structure is formed when an else-if structure is used before the else statement. We will have to check for that.
    // The following code snippet:


    // Generates this tree (see the hierarchy):

}
